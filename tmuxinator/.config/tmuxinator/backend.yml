name: backend
root: ~

windows:
  - code:
      root: ~/projects/jyp
      panes:
        - nvim .
  - git:
      root: ~/projects/jyp
      panes:
        - LazyGit
  - docker:
      root: ~/projects/jyp
      panes:
        - aws-vault exec qa -- docker compose -f docker/docker-compose.local.yml up
